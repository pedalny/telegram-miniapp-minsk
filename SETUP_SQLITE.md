# Использование SQLite (без установки PostgreSQL)

## Преимущества SQLite

✅ **Не требует установки** - встроена в Python  
✅ **Работает сразу** - файл базы данных создается автоматически  
✅ **Идеально для разработки** - быстро и просто  
✅ **Достаточно для небольших проектов** - поддерживает тысячи записей  

## Настройка

### 1. Создайте файл .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
DB_TYPE=sqlite
DATABASE_URL=sqlite:///./minsk_jobs.db
TELEGRAM_BOT_TOKEN=ваш_токен_бота
YANDEX_MAPS_API_KEY=ваш_ключ_яндекс
SECRET_KEY=любая_случайная_строка
```

**Важно:** Для SQLite не нужно ничего устанавливать! База данных создастся автоматически в файле `minsk_jobs.db` в корне проекта.

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

Обратите внимание: `psycopg2-binary` закомментирован в `requirements.txt`, так как он нужен только для PostgreSQL.

### 3. Запустите сервер

```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

При первом запуске автоматически создастся файл `minsk_jobs.db` с таблицами.

## Где находится база данных?

Файл базы данных `minsk_jobs.db` будет создан в корне проекта (там же, где `backend/` и `frontend/`).

## Переход на PostgreSQL (если понадобится)

Если в будущем захотите перейти на PostgreSQL:

1. Установите PostgreSQL
2. Создайте базу данных
3. В `.env` измените:
   ```env
   DB_TYPE=postgresql
   DATABASE_URL=postgresql://postgres:пароль@localhost:5432/minsk_jobs_db
   ```
4. Установите драйвер PostgreSQL:
   ```bash
   pip install psycopg2-binary
   ```
5. Перезапустите сервер

**Примечание:** Данные из SQLite не переносятся автоматически. Нужно будет экспортировать и импортировать вручную.

## Ограничения SQLite

- Менее производительна при большом количестве одновременных запросов (но для вашего проекта этого достаточно)
- Не поддерживает некоторые продвинутые функции PostgreSQL
- Файл базы данных нужно регулярно бэкапить

Для вашего проекта (Telegram Mini App для подработок) SQLite полностью подходит! 🚀

