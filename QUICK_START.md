# Быстрый старт

## Пошаговая инструкция для первого запуска

### 1. Выберите базу данных

**Вариант А: SQLite (рекомендуется для начала)**
- ✅ Не требует установки
- ✅ Работает сразу
- Следуйте инструкции в файле `SETUP_SQLITE.md`

**Вариант Б: PostgreSQL (если доступен)**
- Следуйте инструкции в файле `INSTALL_POSTGRESQL.md`
- Создайте базу данных: `CREATE DATABASE minsk_jobs_db;`

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте файл .env

Создайте файл `.env` в корне проекта.

**Для SQLite (рекомендуется):**
```env
DB_TYPE=sqlite
DATABASE_URL=sqlite:///./minsk_jobs.db
TELEGRAM_BOT_TOKEN=пока_оставьте_пустым
YANDEX_MAPS_API_KEY=пока_оставьте_пустым
SECRET_KEY=любая_случайная_строка_для_безопасности
```

**Для PostgreSQL:**
```env
DB_TYPE=postgresql
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/minsk_jobs_db
TELEGRAM_BOT_TOKEN=пока_оставьте_пустым
YANDEX_MAPS_API_KEY=пока_оставьте_пустым
SECRET_KEY=любая_случайная_строка_для_безопасности
```

**Замените:**
- `ВАШ_ПАРОЛЬ` на пароль PostgreSQL (только если используете PostgreSQL)

### 4. Создайте Telegram бота

Следуйте инструкции в файле `SETUP_TELEGRAM_BOT.md`

**Кратко:**
- Найдите @BotFather в Telegram
- Отправьте `/newbot`
- Введите имя и username бота
- Скопируйте токен и добавьте в `.env` файл

### 5. Получите API ключ Яндекс.Карт

Следуйте инструкции в файле `SETUP_YANDEX_MAPS.md`

**Кратко:**
- Зайдите на https://developer.tech.yandex.ru/
- Создайте приложение
- Получите API ключ для JavaScript API
- Добавьте ключ в `.env` файл

### 6. Добавьте API ключ в HTML

Откройте `frontend/index.html` и найдите строку:

```html
<script src="https://api-maps.yandex.ru/2.1/?apikey=YOUR_YANDEX_API_KEY&lang=ru_RU" type="text/javascript"></script>
```

Замените `YOUR_YANDEX_API_KEY` на ваш реальный ключ.

### 7. Запустите сервер

```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Или из папки backend:

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 8. Проверьте работу

Откройте в браузере: `http://localhost:8000`

Должна открыться карта Минска.

### 9. Подключите к Telegram (опционально, для тестирования)

Для локальной разработки используйте ngrok:

1. Скачайте ngrok: https://ngrok.com/download
2. Запустите: `ngrok http 8000`
3. Скопируйте HTTPS URL (например, `https://abc123.ngrok.io`)
4. В BotFather отправьте `/newapp` и укажите этот URL

Теперь можно открыть приложение через бота в Telegram!

## Проверка работоспособности

### Проверка API

Откройте: `http://localhost:8000/api/health`

Должен вернуться: `{"status": "ok"}`

### Проверка базы данных

При первом запуске сервера таблицы создадутся автоматически. 

**Для SQLite:** Файл `minsk_jobs.db` создастся автоматически в корне проекта.

**Для PostgreSQL:** Если нужно создать вручную:
```bash
python -c "from backend.database import init_db; init_db()"
```

## Частые проблемы

### Ошибка подключения к базе данных (PostgreSQL)

- Проверьте, что PostgreSQL запущен
- Проверьте правильность пароля в `.env`
- Проверьте, что база данных `minsk_jobs_db` создана

### Ошибка подключения к базе данных (SQLite)

- Убедитесь, что в `.env` указано `DB_TYPE=sqlite`
- Проверьте права на запись в папку проекта (для создания файла `minsk_jobs.db`)

### Карта не загружается

- Проверьте, что API ключ Яндекс.Карт добавлен в `frontend/index.html`
- Проверьте консоль браузера (F12) на наличие ошибок

### Ошибка авторизации Telegram

- Проверьте, что токен бота указан в `.env`
- Для локального тестирования без Telegram можно временно отключить проверку (не рекомендуется)

## Следующие шаги

После успешного запуска:

1. Протестируйте создание задачи (красный маркер)
2. Протестируйте создание объявления исполнителя (зеленый маркер)
3. Проверьте просмотр объявлений
4. Проверьте раздел "Мои объявления"

Удачи! 🚀

